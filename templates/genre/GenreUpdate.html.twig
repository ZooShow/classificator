{% extends 'base.html.twig' %}

{% block body %}
    {{ form(form) }}
    <ul>
        {% for sign in signs %}
            <li>
                {{ sign.name }}: {{ sign.value }}
            </li>
        {% endfor %}
    </ul>
{% endblock %}